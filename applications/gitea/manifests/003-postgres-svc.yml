---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: gitea-manifests
  labels:
    app.kubernetes.io/name: gitea
    app.kubernetes.io/component: postgres
spec:
  type: ClusterIP
  ports:
    - name: postgres
      port: 5432
  selector:
    app.kubernetes.io/name: gitea
    app.kubernetes.io/component: postgres
