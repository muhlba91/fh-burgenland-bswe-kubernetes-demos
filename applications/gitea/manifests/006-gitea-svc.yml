---
apiVersion: v1
kind: Service
metadata:
  name: gitea
  namespace: gitea-manifests
  labels:
    app.kubernetes.io/name: gitea
    app.kubernetes.io/component: gitea
spec:
  type: NodePort
  ports:
    - name: http
      port: 3000
    - name: ssh
      port: 22
  selector:
    app.kubernetes.io/name: gitea
    app.kubernetes.io/component: gitea
